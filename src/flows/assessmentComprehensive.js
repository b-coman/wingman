// /src/flows/doAssessmentComprehensive.js

const { sendEmailNotification } = require('../services/emailService');
//const callWingmanAgentsApp = require('../services/wingmanAgentService');

const doAssessmentComprehensive = async (assessmentId) => {
   

    // Additional logic as needed for the General assessment flow
};

module.exports = doAssessmentComprehensive;
